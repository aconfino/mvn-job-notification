This notification plugin sends the following data via JSON to one or more endpoints:

Jenkins job name
Build number 
Jenkins server url
Jenkins job url
Build url
Job workspace
Job status 
(and for Maven projects)
Group Id
Artifact Id
Version

There are several configuration options included only sending notification for successful builds and only sending notification for Maven builds.
